name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'  # Tout le repo = site statique

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Notification Discord
        if: always()
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: '🌐 Déploiement GitHub Pages {{ if eq job.status "success" }}réussi ! 🎉{{ else }}échoué 😢{{ end }} Lien : ${{ steps.deployment.outputs.page_url }}'